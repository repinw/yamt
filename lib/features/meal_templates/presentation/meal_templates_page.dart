import 'package:flutter/material.dart';
import 'package:yamt/features/inventory/presentation/'
    'prepared_meal_templates_page.dart';

class MealTemplatesPage extends StatelessWidget {
  const MealTemplatesPage({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO(meal_templates): Replace the legacy inventory template page with
    // the dedicated meal_templates implementation.
    return const PreparedMealTemplatesPage();
  }
}
