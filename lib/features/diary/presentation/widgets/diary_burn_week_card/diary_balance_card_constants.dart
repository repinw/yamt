/// Outer radius for the diary Burn Week card.
const diaryBalanceCardRadius = 32.0;

/// Height of the Burn Week progress track.
const diaryBalanceProgressHeight = 30.0;

/// Size of the consumed kcal marker icon.
const diaryBalanceFlameIconSize = 24.0;

/// Width of the target marker.
const diaryBalanceTargetMarkerWidth = 3.0;

/// Full progress area height including marker overflow.
const diaryBalanceProgressAreaHeight = 62.0;

/// Vertical inset for the safe-zone fill.
const diaryBalanceSafeZoneVerticalInset = 4.0;

/// Target marker overflow above the progress track.
const diaryBalanceTargetMarkerOverflowTop = 8.0;

/// Fixed stat tile height.
const diaryBalanceStatTileHeight = 104.0;

/// Burn Week game header height.
const diaryBalanceGameHeaderHeight = 24.0;

/// Counter badge height.
const diaryBalanceCounterBadgeHeight = 22.0;

/// Counter badge icon size.
const diaryBalanceCounterIconSize = 14.0;

/// Default ticker period for minute-sensitive Burn Week UI updates.
const diaryBalanceTickerPeriod = Duration(minutes: 1);
