/// Height of the Burn Week progress track.
const diaryBalanceProgressHeight = 20.0;

/// Width of the target marker.
const diaryBalanceTargetMarkerWidth = 2.0;

/// Full progress area height including marker overflow.
const diaryBalanceProgressAreaHeight = 80.0;

/// Fallback width when the progress bar is measured without bounded width.
const diaryBalanceProgressFallbackWidth = 320.0;

/// Target marker overflow above the progress track.
const diaryBalanceTargetMarkerOverflowTop = 18.0;

/// Width reserved for the target marker label.
const diaryBalanceTargetLabelWidth = 54.0;
